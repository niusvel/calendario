# Entorno virtual de Python
.venv/
venv/
env/

# Bytecode / caches
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Configuracion PRIVADA: contiene las URLs reales de iCloud (webcal://).
# NO debe subirse al repositorio.
config.yaml

# (El export privado de la familia se ignora globalmente en el .gitignore raiz.)

# Variables de entorno
.env
.env.*
